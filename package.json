{
    "name": "my-app",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "axios": "^1.1.3",
        "json-server": "^0.17.0",
        "react": "^16.9.0",
        "react-dom": "^16.9.0",
        "react-router-dom": "^6.4.2",
        "react-scripts": "3.1.2",
        "web-vitals": "^2.1.0"
    },
    "scripts": {
        "start": "react-scripts --openssl-legacy-provider start",
        "build": "react-scripts build",
        "test": "react-scripts test",
        "eject": "react-scripts eject",
        "db": "npx json-server -p 3001 --watch src/db/db.json"
    },
    "eslintConfig": {
        "extends": [
            "react-app",
            "react-app/jest"
        ]
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
